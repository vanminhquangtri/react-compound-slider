import React from 'react';
import {ValidateableForm} from 'react-form-validate'
console.log('ValidateableForm :>> ', ValidateableForm);

const Validated = () => {
    return (
        <div>
            
        </div>
    );
};

export default Validated;